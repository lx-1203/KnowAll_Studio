# KnowAll Studio - Backend Dependencies
# Python 3.11+

# Web Framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Database
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20.0
asyncmy>=0.2.9           # MySQL/MariaDB async driver (production)
asyncpg>=0.29.0           # PostgreSQL for team edition

# Vector Database
chromadb>=0.5.0
sentence-transformers>=3.0.0

# File Parsing
PyMuPDF>=1.24.0       # PDF parsing
python-docx>=1.1.0    # Word parsing
python-pptx>=0.6.23   # PowerPoint parsing
Pillow>=10.3.0        # Image processing
pytesseract>=0.3.10   # OCR (optional, requires Tesseract installed)
PaddleOCR>=2.8.0      # Chinese OCR (optional)
xmindparser>=1.0.0    # Xmind parsing
docling>=2.0          # Structured document parsing with heading/image/table extraction
hierarchical-pdf      # Fix PDF heading hierarchy flattening in Docling

# API / HTTP
httpx>=0.27.0
tiktoken>=0.7.0

# Reading Language (optional, for offline dictionary fallback)
jieba>=0.42.0

# Prompt Templates
pyyaml>=6.0

# Configuration
pydantic-settings>=2.2.0

# Auth / Crypto
python-jose[cryptography]>=3.3.0
cryptography>=42.0.0
bcrypt>=4.0.0

# Database migrations
alembic>=1.18.0

# Anki import/export (optional)
# genanki>=0.13.0

# Development
pytest>=8.2.0
pytest-asyncio>=0.23.0
