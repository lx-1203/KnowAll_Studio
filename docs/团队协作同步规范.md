# 团队协作实时同步文档规范

> **仓库地址**：[https://github.com/lx-1203/KnowAll_Studio](https://github.com/lx-1203/KnowAll_Studio)  
> **负责人**：[@lx-1203](https://github.com/lx-1203)  
> **版本**：v1.0 · 2026-06-28

---

## 目录

- [一、实时同步总览](#一实时同步总览)
- [二、实时文档协作（WebSocket 同步）](#二实时文档协作websocket-同步)
- [三、实时文件上传/下载同步](#三实时文件上传下载同步)
- [四、消息通知实时推送](#四消息通知实时推送)
- [五、冲突处理与版本管理](#五冲突处理与版本管理)
- [六、离线与断线重连](#六离线与断线重连)
- [附：Git 协作简要规范](#附git-协作简要规范)

---

## 一、实时同步总览

### 1.1 同步场景

| 场景 | 同步方式 | 延迟 | 核心要求 |
|------|----------|------|----------|
| **协作文档编辑** | WebSocket（CRDT/OT） | < 500ms | 多人同时编辑不丢数据、不互相覆盖 |
| **文件上传/下载** | HTTP 分片上传 + WebSocket 广播通知 | 秒级 | 大文件不阻塞，进度实时可见 |
| **消息/通知** | WebSocket / SSE | < 200ms | 低延迟，送达保证 |
| **任务状态变更** | WebSocket 广播 | < 500ms | 同空间所有人即时看到 |
| **Git 代码同步** | Git push/pull + PR | 手动 | 见附录简要规范 |

### 1.2 核心原则

1. **谁改谁推送**：任何成员的数据变更（编辑、上传、删除），由**发起方客户端**向服务端 push，服务端广播给同空间其他成员。
2. **增量同步**：只传输变更部分（diff / delta），不传全量数据。
3. **版本为准**：所有数据带单调递增版本号，客户端发现版本落后则拉取更新，发现版本冲突则走 OT/CRDT 合并。
4. **离线可恢复**：断线期间的修改本地缓存，重连后自动重放。

---

## 二、实时文档协作（WebSocket 同步）

### 2.1 架构

```
┌──────────┐  WebSocket   ┌─────────────────┐   Pub/Sub   ┌──────────┐
│  成员 A   │◄────────────►│   后端同步服务    │◄──────────►│  Redis    │
│ (编辑器)  │  wss://...   │  (Collaboration  │            │ (消息广播) │
└──────────┘              │   Server)       │            └──────────┘
                          │                 │
┌──────────┐  WebSocket   │  房间管理        │
│  成员 B   │◄────────────►│  操作转换(OT)    │
│ (编辑器)  │              │  版本控制        │
└──────────┘              └─────────────────┘
```

### 2.2 通信协议

#### 连接建立

```javascript
// 客户端连接
const ws = new WebSocket('wss://server/sync?doc_id=123&user_id=xxx&token=xxx');

// 服务端验证 token → 加入文档房间 → 返回当前文档状态
```

#### 消息格式（统一 JSON 协议）

```json
{
  "type": "operation",          // 消息类型
  "doc_id": "doc_123",          // 文档 ID
  "user_id": "u_01",            // 操作者
  "user_name": "张三",           // 操作者名称（显示用）
  "version": 42,                // 基于哪个版本的操作
  "timestamp": 1719561600000,   // 操作时间戳（毫秒）
  "data": { ... }               // 具体操作载荷
}
```

#### 消息类型定义

| type | 方向 | 说明 | data 载荷 |
|------|------|------|-----------|
| `operation` | C→S→C | 编辑操作 | 见 2.3 操作类型 |
| `ack` | S→C | 服务端确认，返回新版本号 | `{ "new_version": 43 }` |
| `sync_full` | S→C | 全量同步（首次连接时） | `{ "content": {...}, "version": 43 }` |
| `sync_diff` | S→C | 增量同步（版本落后时） | `[{ "op": "insert", ... }, ...]` |
| `cursor` | C→S→C | 光标位置广播 | `{ "row": 10, "col": 5 }` |
| `presence` | S→C | 在线状态变更 | `{ "online_users": [...] }` |
| `heartbeat` | C→S | 心跳 | `{}` |
| `error` | S→C | 错误 | `{ "code": "VERSION_CONFLICT", "msg": "..." }` |

### 2.3 编辑操作类型（OT 操作）

采用 **JSON0** 风格的 OT 操作格式：

#### 文本编辑操作

```json
// 插入文本（位置 10 处插入 "hello"）
{
  "type": "operation",
  "operation": "insert",
  "path": ["content", "paragraphs", 3, "text"],
  "position": 10,
  "value": "hello"
}

// 删除文本（位置 5~8 的 3 个字符）
{
  "type": "operation",
  "operation": "delete",
  "path": ["content", "paragraphs", 3, "text"],
  "position": 5,
  "length": 3
}

// 属性修改（第 3 段加粗）
{
  "type": "operation",
  "operation": "set_attribute",
  "path": ["content", "paragraphs", 3],
  "attribute": "bold",
  "value": true
}
```

#### 结构化操作（列表、表格、任务）

```json
// 列表新增项
{ "operation": "list_insert", "list_id": "tasks", "index": 2, "item": { ... } }

// 列表删除项
{ "operation": "list_delete", "list_id": "tasks", "index": 2 }

// 列表移动项
{ "operation": "list_move", "list_id": "tasks", "from": 2, "to": 4 }

// 字段更新
{ "operation": "field_update", "item_id": "task_42", "field": "status", "value": "done" }
```

### 2.4 OT 操作转换流程

```
        服务端当前版本: V=42
                    │
    ┌───────────────┼───────────────┐
    ▼               ▼               ▼
  A 发操作        B 发操作        C 发操作
  (based V=42)   (based V=42)   (based V=42)
    │               │               │
    ▼               ▼               ▼
  服务端 OT 变换（按到达顺序）：
  
  ① A 先到 → 直接应用 → 版本变为 43
  ② B 到达 → 基于 V=42，但当前已是 V=43
           → OT 变换 B' = transform(B, A)
           → 以 B' 应用到 V=43 → 版本变为 44
  ③ C 到达 → 基于 V=42，当前已是 V=44
           → OT 变换 C' = transform(C, [A, B'])
           → 以 C' 应用到 V=44 → 版本变为 45
    │
    ▼
  广播给所有客户端：{ type: "operation", ..., version: 45 }
```

**核心逻辑（伪代码）：**

```javascript
function handleOperation(clientOp, currentDoc) {
  // 1. 如果客户端版本落后，做 OT 变换
  let transformedOp = clientOp;
  const opsToTransform = getOpsSince(clientOp.version); // 服务端已应用的后续操作
  
  for (const appliedOp of opsToTransform) {
    // 双向变换：同时变换 clientOp 和 appliedOp
    const [opA, opB] = OT.transform(transformedOp, appliedOp);
    transformedOp = opA;  // 变换后的 clientOp 应用到当前状态
    // appliedOp 已在文档中，不需要更新（但如果要支持 undo 则需要 opB）
  }
  
  // 2. 应用变换后的操作
  currentDoc.apply(transformedOp);
  currentDoc.version++;
  
  // 3. 广播给房间内其他客户端
  broadcast(room, {
    type: "operation",
    ...transformedOp,
    version: currentDoc.version,
    user_id: clientOp.user_id
  });
  
  // 4. 回 ack 给发送者
  sendAck(clientOp.user_id, { new_version: currentDoc.version });
}
```

### 2.5 客户端处理流程

```javascript
class RealtimeDocClient {
  constructor(docId, userId) {
    this.docId = docId;
    this.userId = userId;
    this.localVersion = 0;     // 本地已同步的版本号
    this.pendingOps = [];      // 待发送队列
    this.offlineQueue = [];    // 离线操作队列
  }

  // 用户编辑 → 生成操作 → 发送
  onUserEdit(operation) {
    const op = {
      type: "operation",
      doc_id: this.docId,
      user_id: this.userId,
      version: this.localVersion,
      timestamp: Date.now(),
      data: operation
    };
    
    // 乐观应用（本地立即生效，不等服务端确认）
    this.applyLocal(op);
    this.send(op);
  }

  // 收到服务端广播（别人的操作或自己的确认）
  onServerMessage(msg) {
    switch (msg.type) {
      case "ack":
        // 自己的操作被确认 → 更新本地版本
        this.localVersion = msg.data.new_version;
        break;
        
      case "operation":
        if (msg.user_id === this.userId) return; // 自己的操作忽略
        // 别人的操作 → 应用到本地编辑器
        this.applyRemote(msg);
        this.localVersion = msg.version;
        break;
        
      case "sync_full":
        // 首次连接 → 全量替换
        this.setContent(msg.data.content);
        this.localVersion = msg.data.version;
        break;
        
      case "sync_diff":
        // 断线重连 → 增量追赶
        for (const op of msg.data.ops) {
          this.applyRemote(op);
        }
        this.localVersion = msg.data.version;
        break;
        
      case "error":
        if (msg.data.code === "VERSION_CONFLICT") {
          // 严重冲突 → 请求全量同步
          this.requestFullSync();
        }
        break;
    }
  }

  send(op) {
    if (this.ws.readyState === WebSocket.OPEN) {
      this.ws.send(JSON.stringify(op));
    } else {
      this.offlineQueue.push(op);  // 离线 → 进队列
    }
  }
}
```

---

## 三、实时文件上传/下载同步

### 3.1 文件上传流程

```
┌─────────┐                    ┌──────────────┐                 ┌─────────┐
│  成员 A  │                    │   后端服务    │                 │ 其他成员 │
└────┬────┘                    └──────┬───────┘                 └────┬────┘
     │                               │                              │
     │ ① POST /upload/init           │                              │
     │   {filename, size, space_id}   │                              │
     │──────────────────────────────►│                              │
     │                               │                              │
     │ ② 返回 upload_id + presigned_url │                           │
     │◄──────────────────────────────│                              │
     │                               │                              │
     │ ③ 分片上传（每片 5MB）         │                              │
     │ PUT /upload/{id}/chunk/{n}    │                              │
     │──────────────────────────────►│                              │
     │ 进度回调: {chunk: 5/20}       │                              │
     │◄──────────────────────────────│                              │
     │  ... (重复直到所有分片上传完)    │                              │
     │                               │                              │
     │ ④ POST /upload/complete       │                              │
     │──────────────────────────────►│                              │
     │                               │ ⑤ 合并分片 → 存储 → 生成 URL  │
     │                               │────────────────────          │
     │                               │                    │         │
     │                               │ ⑥ WebSocket 广播文件上传事件 │
     │                               │─────────────────────────────►│
     │                               │  {                          │
     │                               │   type: "file_uploaded",    │
     │                               │   file_id, filename,        │
     │                               │   size, url, version: 1     │
     │                               │  }                          │
     │                               │                    │         │
     │                               │             ⑦ 收到通知      │
     │                               │           → 更新文件列表UI  │
     │                               │           → 可点下载        │
```

### 3.2 上传 API 规范

#### 3.2.1 初始化上传

```
POST /api/upload/init
Content-Type: application/json

请求：
{
  "filename": "复习笔记.pdf",
  "file_size": 52428800,        // 字节
  "mime_type": "application/pdf",
  "space_id": "team_space_01",  // 协作空间 ID
  "parent_id": "folder_01"      // 目标文件夹（可选）
}

响应：
{
  "code": 0,
  "data": {
    "upload_id": "upload_abc123",
    "chunk_size": 5242880,      // 每片 5MB
    "total_chunks": 10,
    "status": "uploading"
  }
}
```

#### 3.2.2 分片上传

```
PUT /api/upload/{upload_id}/chunk/{chunk_index}
Content-Type: application/octet-stream
X-Chunk-Hash: sha256=<分片哈希>

响应：
{
  "code": 0,
  "data": {
    "upload_id": "upload_abc123",
    "chunk_index": 5,
    "received": 26214400,       // 已接收字节
    "total": 52428800,
    "progress": 0.5
  }
}
```

#### 3.2.3 完成上传

```
POST /api/upload/{upload_id}/complete

响应：
{
  "code": 0,
  "data": {
    "file_id": "file_xyz789",
    "filename": "复习笔记.pdf",
    "size": 52428800,
    "url": "https://storage/files/file_xyz789",
    "version": 1,
    "created_at": "2026-06-28T10:30:00Z"
  }
}
```

### 3.3 文件下载流程

```
成员 B 点击下载
     │
     ▼
GET /api/download/{file_id}
     │
     ▼
后端返回：302 → 预签名下载 URL（有效期 10 分钟）
或 200 → 直接流式传输（小文件）
     │
     ▼
客户端接收文件，展示下载完成
```

```
GET /api/download/{file_id}

响应（小文件 < 10MB）：
HTTP 200
Content-Type: application/octet-stream
Content-Disposition: attachment; filename="复习笔记.pdf"
<文件二进制流>

响应（大文件）：
HTTP 302
Location: https://storage/presigned/xxx?expires=...
```

### 3.4 文件同步通知协议（WebSocket）

#### 文件上传完成通知

```json
{
  "type": "file_uploaded",
  "space_id": "team_space_01",
  "data": {
    "file_id": "file_xyz789",
    "filename": "复习笔记.pdf",
    "size": 52428800,
    "mime_type": "application/pdf",
    "url": "/api/download/file_xyz789",
    "version": 1,
    "uploaded_by": "u_01",
    "uploaded_by_name": "张三",
    "parent_id": "folder_01",
    "timestamp": 1719561600000
  }
}
```

#### 文件删除通知

```json
{
  "type": "file_deleted",
  "space_id": "team_space_01",
  "data": {
    "file_id": "file_xyz789",
    "filename": "复习笔记.pdf",
    "deleted_by": "u_02",
    "deleted_by_name": "李四",
    "timestamp": 1719561800000
  }
}
```

#### 文件更新通知（同名覆盖/新版本）

```json
{
  "type": "file_updated",
  "space_id": "team_space_01",
  "data": {
    "file_id": "file_xyz789",
    "filename": "复习笔记.pdf",
    "old_version": 1,
    "new_version": 2,
    "size": 53000000,
    "url": "/api/download/file_xyz789",
    "updated_by": "u_03",
    "updated_by_name": "王五",
    "timestamp": 1719562000000
  }
}
```

### 3.5 客户端文件同步模块

```javascript
class FileSyncClient {
  constructor(spaceId, userId) {
    this.spaceId = spaceId;
    this.userId = userId;
    this.fileList = [];          // 本地文件列表缓存
  }

  // ========== 上传 ==========

  async uploadFile(file, parentId = null) {
    // ① 初始化上传
    const initResp = await fetch('/api/upload/init', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        filename: file.name,
        file_size: file.size,
        mime_type: file.type,
        space_id: this.spaceId,
        parent_id: parentId
      })
    });
    const { upload_id, chunk_size, total_chunks } = (await initResp.json()).data;

    // ② 分片上传（带进度回调）
    for (let i = 0; i < total_chunks; i++) {
      const start = i * chunk_size;
      const chunk = file.slice(start, start + chunk_size);
      await fetch(`/api/upload/${upload_id}/chunk/${i}`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/octet-stream' },
        body: chunk
      });
      this.onProgress({ chunk: i + 1, total: total_chunks });
    }

    // ③ 完成上传
    const completeResp = await fetch(`/api/upload/${upload_id}/complete`, {
      method: 'POST'
    });
    const fileInfo = (await completeResp.json()).data;

    // ④ 更新本地列表（等待 WebSocket 广播确认）
    this.onFileUploaded(fileInfo);
    return fileInfo;
  }

  // ========== 下载 ==========

  async downloadFile(fileId) {
    const resp = await fetch(`/api/download/${fileId}`);
    if (resp.status === 302) {
      window.open(resp.headers.get('Location'), '_blank');
    } else {
      const blob = await resp.blob();
      const disposition = resp.headers.get('Content-Disposition');
      const filename = disposition?.match(/filename="(.+)"/)?.[1] || 'download';
      this.saveBlob(blob, filename);
    }
  }

  saveBlob(blob, filename) {
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = filename;
    a.click();
    URL.revokeObjectURL(url);
  }

  // ========== WebSocket 事件处理 ==========

  onWsMessage(msg) {
    switch (msg.type) {
      case 'file_uploaded':
        // 队友上传了文件 → 更新文件列表 UI
        this.fileList.push(msg.data);
        this.onFileUploaded(msg.data);
        break;

      case 'file_deleted':
        // 队友删除了文件 → 从列表移除
        this.fileList = this.fileList.filter(f => f.file_id !== msg.data.file_id);
        this.onFileDeleted(msg.data);
        break;

      case 'file_updated':
        // 队友更新了文件 → 更新列表信息
        const file = this.fileList.find(f => f.file_id === msg.data.file_id);
        if (file) {
          file.version = msg.data.new_version;
          file.size = msg.data.size;
        }
        this.onFileUpdated(msg.data);
        break;
    }
  }
}
```

### 3.6 上传进度实时同步

```json
// 上传者 → 服务端 → 同空间其他人（可选，用于协作空间中展示"XX 正在上传"）
{
  "type": "upload_progress",
  "space_id": "team_space_01",
  "data": {
    "user_id": "u_01",
    "user_name": "张三",
    "filename": "复习笔记.pdf",
    "progress": 0.65,
    "status": "uploading"   // "uploading" | "completed" | "failed"
  }
}
```

---

## 四、消息通知实时推送

### 4.1 消息通道

```
┌──────────────────────────────────────────┐
│              后端消息服务                  │
│                                          │
│  WebSocket 长连接（实时推送）              │
│  ├─ 协作编辑通知（operation）             │
│  ├─ 文件变更通知（file_*）               │
│  ├─ 即时消息（chat_message）             │
│  ├─ 系统通知（system_notify）            │
│  └─ 在线状态（presence）                 │
│                                          │
│  SSE（Server-Sent Events）备选通道        │
│  └─ 单向推送，无需双向通信时使用           │
└──────────────────────────────────────────┘
```

### 4.2 消息协议

```json
// 即时消息
{
  "type": "chat_message",
  "space_id": "team_space_01",
  "data": {
    "msg_id": "msg_001",
    "from_user_id": "u_01",
    "from_user_name": "张三",
    "content": "复习笔记已上传，大家看一下第三章",
    "content_type": "text",        // "text" | "image" | "file_ref"
    "file_ref": null,              // 引用文件时填 file_id
    "timestamp": 1719561600000
  }
}

// 系统通知（管理员操作、权限变更等）
{
  "type": "system_notify",
  "space_id": "team_space_01",
  "data": {
    "notify_id": "sys_001",
    "level": "info",              // "info" | "warning" | "error"
    "title": "新成员加入",
    "content": "李四 加入了协作空间",
    "timestamp": 1719561600000
  }
}

// 在线状态
{
  "type": "presence",
  "space_id": "team_space_01",
  "data": {
    "online_users": [
      { "user_id": "u_01", "user_name": "张三", "status": "editing" },
      { "user_id": "u_02", "user_name": "李四", "status": "viewing" }
    ],
    "total_online": 2
  }
}
```

### 4.3 推送可靠性保证

| 机制 | 说明 |
|------|------|
| **Seq 序列号** | 每条消息带单调递增 seq，客户端检测 gap 则补拉 |
| **ACK 确认** | 关键消息（文件上传完成等）客户端返回 ack，服务端未收到则重推 |
| **消息持久化** | 用户离线期间的消息写入 Redis list，上线后批量推送 |
| **去重** | 客户端按 msg_id 去重，防止重复推送 |

---

## 五、冲突处理与版本管理

### 5.1 版本号规则

```
每个协作资源（文档/文件夹/任务列表）独立维护版本号
版本号单调递增，格式：{resource_type}:{resource_id}:{version_num}
示例：doc:123:v42

每次成功写入 → version += 1
客户端操作时必须附带 base_version
```

### 5.2 冲突场景与处理

| 场景 | 触发条件 | 处理方式 |
|------|----------|----------|
| **编辑冲突** | 两人基于同一版本修改同一位置 | OT 变换，自动合并（见 2.4） |
| **文件同名上传** | B 上传了和 A 同名的文件 | 自动加版本后缀：`笔记_v2.pdf`，两文件共存 |
| **文件覆盖更新** | B 对 A 刚上传的文件发起"更新" | 版本号校验：B 必须基于最新版本。基于旧版本的更新被拒绝，提示先刷新 |
| **同时删除** | A 和 B 同时删同一文件 | 先到达服务端的删除生效，后到达的返回 "already deleted" |
| **版本严重分叉** | 离线很久后上线，版本落后太多 | 放弃增量追赶，触发全量同步（sync_full） |

### 5.3 文件版本历史

```
每个文件保留最近 10 个版本：
  file_xyz789
    ├─ v1: 张三上传 "复习笔记.pdf" (2026-06-20)
    ├─ v2: 李四更新 "复习笔记_v2.pdf" (2026-06-22)
    ├─ v3: 张三更新 "复习笔记_v3.pdf" (2026-06-25)
    └─ v4: 王五更新 "复习笔记_v4.pdf" (2026-06-28) ← 当前版本

用户可查看历史版本列表，可回滚到任一历史版本。
回滚 = 以历史版本内容创建一个新版本（v5）。
```

```json
// GET /api/file/{file_id}/versions
{
  "code": 0,
  "data": {
    "file_id": "file_xyz789",
    "current_version": 4,
    "versions": [
      { "version": 4, "updated_by": "王五", "timestamp": 1719561600000, "size": 53000000 },
      { "version": 3, "updated_by": "张三", "timestamp": 1719300000000, "size": 52000000 },
      { "version": 2, "updated_by": "李四", "timestamp": 1719000000000, "size": 51000000 },
      { "version": 1, "updated_by": "张三", "timestamp": 1718800000000, "size": 50000000 }
    ]
  }
}

// POST /api/file/{file_id}/rollback
// Body: { "target_version": 2 }
// 效果：创建 v5，内容 = v2 的内容
```

---

## 六、离线与断线重连

### 6.1 心跳与断线检测

```
客户端：每 15 秒发送 ❤ ping
服务端：超过 30 秒未收到 ping → 判定离线
       → 广播 presence 更新给同空间其他人
       → 将该用户标记为 "offline"，开始缓存消息

客户端：WebSocket onclose 触发
       → 3 秒后首次重连
       → 指数退避：3s → 6s → 12s → 24s → ... → 最大 60s
       → 重连成功后比较本地版本号与服务端版本号
```

### 6.2 重连同步流程

```
客户端重连成功
  │
  ├─ 发送：{ type: "reconnect", doc_id: "xxx", local_version: 40 }
  │
  ▼
服务端比对：
  local_version(40) vs server_version(45)
  │
  ├─ 40 == 45 → 无需同步，继续正常通信
  ├─ 40 < 45 且差距 ≤ 50  → 发送增量 ops（sync_diff，v41~v45 的 5 个操作）
  └─ 40 < 45 且差距 > 50   → 发送全量快照（sync_full）
```

### 6.3 离线操作队列

```javascript
// 客户端离线期间
class OfflineQueue {
  constructor() {
    this.queue = [];            // 按操作顺序存储
    this.maxQueueSize = 500;    // 最大缓存 500 个操作，超出则合并
  }

  push(op) {
    // 尝试合并相邻同类型操作（如连续文本插入合并为一条）
    const last = this.queue[this.queue.length - 1];
    if (last && this.canMerge(last, op)) {
      this.queue[this.queue.length - 1] = this.merge(last, op);
    } else {
      this.queue.push(op);
    }
  }

  // 重连后批量重放
  async replay(sendFn) {
    while (this.queue.length > 0) {
      const op = this.queue.shift();
      await sendFn(op);
    }
  }

  canMerge(a, b) {
    // 同用户、同文档、同类型的连续 insert 可合并
    return a.type === 'insert' && b.type === 'insert' &&
           a.doc_id === b.doc_id &&
           a.path.join('.') === b.path.join('.') &&
           a.position + a.value.length === b.position;
  }

  merge(a, b) {
    return { ...a, value: a.value + b.value };
  }
}
```

### 6.4 消息持久化与补拉

```
服务端 Redis 结构：

┌─────────────────────────────────────────────┐
│  user:{uid}:offline_msgs  →  List           │
│  离线消息队列，用户上线后批量弹出            │
│  最大长度 200，超出则丢弃最老的             │
│                                             │
│  room:{rid}:event_log  →  Sorted Set        │
│  房间事件日志，按 seq 排序                  │
│  保留最近 1000 条                           │
│  客户端带 last_seq 请求补拉                 │
│                                             │
│  user:{uid}:presence  →  String             │
│  在线状态，带 TTL（30 秒过期）              │
└─────────────────────────────────────────────┘
```

```javascript
// 客户端重连后补拉消息
async function catchUp(lastSeq) {
  const resp = await fetch(`/api/events/catchup?space_id=${spaceId}&from_seq=${lastSeq}`);
  const { events } = await resp.json();
  for (const event of events) {
    this.handleEvent(event);
  }
}
```

---

## 附：Git 代码同步规范 —— 杜绝分叉屎山

> **核心目标：每个人的每一行代码，必须 push 到 GitHub、必须合并回 dev、必须与全队代码兼容。不允许出现"我本地能跑就行"的情况。**

### 什么是"屎山"？怎么形成的？

```
第 1 天：
  dev:  A──B──C
  张三:        └──D──E──F──G──H──I  (本地囤了 6 个 commit 不 push)
  李四:        └──J──K──L──M        (本地囤了 4 个 commit 不 push)

第 3 天（要合并了）：
  dev:  A──B──C
  张三:        └──D──E──F──G──H──I  ← 和 dev 差距 50 个 commit
  李四:        └──J──K──L──M──N──O  ← 大量冲突
  
  结果：合并时冲突遍地，张三覆盖了李四的代码，功能互相不兼容。
       → 这就是"屎山"。
```

### 正确做法

```
每天：
  张三: 做一点 → commit → push → 提 PR → 合并回 dev  ──┐
  李四: 做一点 → commit → push → 提 PR → 合并回 dev  ──┤
                                                         │
  dev 始终是最新、完整、可运行的代码 ◄────────────────────┘

  所有人永远基于最新 dev 开分支 → 不会有大量分叉 → 冲突永远小范围
```

### 强制规则

#### 规则 1：每次改动必须 push，绝不囤本地

```
✅ 每天 push 至少 1 次，最好每 1~2 小时 push 一次
✅ 下班前 push 所有 commit，不能留任何代码在本地
❌ "我先在本地写一周，写完再一起推" — 这就是屎山的起点
```

```bash
# 开发中每完成一个子任务：
git add <改了哪些文件>
git commit -m "feat(模块): 做了什么"
git pull origin dev          # 拉最新 → 有冲突立即解决
git push origin <我的分支>    # 推到 GitHub
```

#### 规则 2：分支必须频繁与 dev 同步，不能分叉太久

```
分支生命周期上限：3 天

超过 3 天未合并回 dev 的分支：
  → 越往后冲突越多
  → 合并时可能发现功能方向和别人完全不兼容
  → 重写成本指数增长
```

```bash
# 每天至少将 dev 的最新改动合并到自己的分支 1 次：
git checkout dev && git pull origin dev
git checkout <我的分支>
git merge dev           # 或 git rebase dev
# 解决冲突 → commit → push
```

#### 规则 3：功能完成当天必须提 PR 合并回 dev

```
功能开发完成 → 当天提 PR → 当天（或次日）Review → 合并回 dev
                                                ↓
                                    你的代码进入了主干
                                    ↓
                              其他同事的 dev 里就有了你的变更
                              他们基于包含你代码的 dev 继续开发
                              → 全队代码始终兼容
```

```bash
# 功能做完了：
git push origin <我的分支>
gh pr create --base dev --head <我的分支> --title "feat(模块): 功能描述"

# 合并后立即删除远程分支（GitHub 会自动提示）
git checkout dev && git pull origin dev && git branch -d <我的分支>
```

#### 规则 4：合并前必须验证兼容性

```
提 PR 前必须：
  1. git pull origin dev（拉最新）
  2. 本地运行全量测试（确保你的改动没破坏别人的功能）
  3. 检查没有覆盖其他人的近期改动
  4. 确认功能在最新 dev 上能正常运行
```

```bash
# PR 前兼容性检查：
git checkout dev && git pull origin dev
git checkout <我的分支>
git merge dev                  # 合并最新 dev
npm test                       # 跑全量测试
# 全部通过 → push → 提 PR
```

#### 规则 5：同一文件被多人修改时，先沟通再动工

```
如果发现 Issue 中有人要改的文件和你的重叠：
  1. 在 Issue 下沟通：谁先合并？
  2. 后合并的人负责解决冲突
  3. 绝对不要两个人同时改同一个文件的不同功能然后硬合
```

### 每日完整流程（贴在显示器旁边）

```
开工：
  git checkout dev && git pull origin dev
  → 看一眼 GitHub Issues，知道自己今天做什么

开发中（每 1~2 小时）：
  git add <具体文件>
  git commit -m "feat(模块): 做了什么"
  git pull origin dev
  git push origin <我的分支>

收工（缺一不可）：
  1. commit + push 所有本地改动
  2. 功能做完了 → 提 PR
  3. 功能没做完 → push 到自己的远程分支，第二天继续
  4. git checkout dev && git pull origin dev（拉全队今日更新）
```

### 屎山自查清单（收工前看一眼）

```
□ 今天写的所有代码都 commit 并 push 了吗？
  → 如果没有，现在就做。硬盘坏了就没了。

□ 我的分支有没有超过 3 天没合并回 dev？
  → 如果超过，明天第一件事提 PR。

□ 我的分支和 dev 的差距有多少个 commit？
  → 跑 git log dev..<我的分支> --oneline | wc -l
  → 超过 10 个 commit 差距 → 立即合并或 rebase

□ 我改的文件有别人也在改吗？
  → 有 → 在 Issue 下沟通合并顺序

□ 合并前在最新 dev 上跑过测试了吗？
  → 没有 → 现在就跑
```

### 红线（违反就是事故）

| 行为 | 后果 |
|------|------|
| 不 push 代码超过 24 小时 | 第二天冲突概率翻倍，硬盘坏了全丢 |
| 功能分支超过 3 天不合并 | 分叉严重，合并成本指数增长 |
| `git push --force` 到 dev/master | 直接覆盖同事代码，不可恢复 |
| 不 pull dev 直接提 PR | PR 可能和 dev 不兼容，Review 被打回 |
| 跳过 Review 直接合 master | 未审核代码进主干，全员风险 |

---

> **本规范自发布之日起生效。**
>
> **问题反馈**：[GitHub Issues](https://github.com/lx-1203/KnowAll_Studio/issues)  
> **仓库地址**：[https://github.com/lx-1203/KnowAll_Studio](https://github.com/lx-1203/KnowAll_Studio)
